eureka:
  client:
    serviceUrl:
      defaultZone: http://peer1:8671/eureka/,http://peer2:8672/eureka/
  instance:
    lease-renewal-interval-in-seconds: 30
    lease-expiration-duration-in-seconds: 90
server:
  port: 8761
spring:
  application:
    name: service-hi
